agents:
  queue: ${PIPELINE_QUEUE:-default}
steps:
  - key: test
    label: ":running: Versions"
    command: sleep 120
    timeout_in_minutes: 1
  - label: dependent
    command: "true"
    allow_dependency_failure: true
    depends_on:
    - test
