agents:
  queue: ${PIPELINE_QUEUE:-default}
  stack: ${PIPELINE_STACK:-*}
steps:
  - command: echo "hello" $Foot $FOO
  - command: 'Get-ChildItem env:'
  - command: bash -c 'echo Foot=$$Foot FOO=$$FOO FOOT=$$FOOT'
