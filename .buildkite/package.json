{
  "type": "module",
  "scripts": {
    "compile": "npx tsc",
    "generate-pipeline": "npm run compile && node pipeline.js"
  },
  "devDependencies": {
    "@triarius/pipeline-schema": "^0.1.0",
    "@types/node": "^18.11.9",
    "js-yaml": "^4.1.0",
    "typescript": "^4.9.3",
    "yaml": "^2.1.3"
  }
}
