#!/usr/bin/env bash

set -Eeufo pipefail

echo -n "$BUILDKITE_AGENT_ACCESS_TOKEN" | sha256sum > token_hash
buildkite-agent meta-data set "agent-access-token-hash-${BUILDKITE_PARALLEL_JOB}" < token_hash

sleep 1
pgrep buildkite-agent
sleep 10
