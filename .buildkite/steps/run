#!/usr/bin/env bash

set -Eeuo pipefail

print_secrets() {
  echo This a secret: 🎉💜👍
}

print_secrets

buildkite-agent secret redact <<SECRETS
🎉💜👍
SECRETS

print_secrets

echo "$MY_VALUE"
