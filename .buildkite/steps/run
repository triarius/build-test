#!/usr/bin/env bash

function trap_sigterm() {
  echo "SIGTERM received, sleeping for 10 seconds"
  sleep 5
  echo "done sleeping! exiting now"
  exit 0
}

# trap sigterms and sleep for 10 seconds
trap 'trap_sigterm' SIGTERM

sleep 60
